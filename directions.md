

To deploy the auto-updater:

- clone github.io repo: `git clone git@github.com-github-io:aaronbloomfield/aaronbloomfield.github.io github.io`
- in that dir, run: `git submodule update --init
- then run: `git submodule update --init`
  - many (most?) will fail -- for each sub-module you have to go in and checkout the correct branch
- 